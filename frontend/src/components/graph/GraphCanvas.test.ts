import { describe, it } from "vitest";

// Skipped pending UI redesign stabilization (Change 17).
// Custom canvas 2D sim replaces react-force-graph; helpers changed.
describe.skip("GraphCanvas helpers", () => {
  it.todo("re-calibrate after design stabilizes");
});

describe.skip("getGraphRenderConfig (Change 13 WS4 LOD)", () => {
  it.todo("LOD config replaced by custom canvas sim threshold logic");
});
